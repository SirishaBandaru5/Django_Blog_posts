# Django_Blog_posts

To run the project follow the steps

1. Clone the repo
2.navigate to the project diorectory in the local terminal
3.run the requirements installation with  :
      pipenv install --dev
4.make sure you have pipenv installed already
5.run the Django server in the project repositoty by
      python manage.py runserver
